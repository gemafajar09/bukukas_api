# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below if you use Go modules)
vendor/

# Go workspace file (if using older versions of Go)
Gopkg.lock
Gopkg.toml

# Go modules cache (should not be committed)
go.sum

# IDE/editor settings
.idea/
.vscode/
*.swp

# Logs
*.log

# OS-specific
.DS_Store
Thumbs.db
